syntax = "v1"

info(
    title: "account"
    desc: "type desc here"
    author: "ocean"
    email: "1252074183@qq.com"
    time: "2024-04-08"
)

// 请求参数结构体
type (
    IdReq {
        Id uint64 `json:"id"`
    }
)

// 返回参数结构体
type (
    AccountResp {
        Id uint64 `json:"id"`
        Account string `json:"account"`
        Phone string `json:"phone"`
        Email string `json:"email"`
    }
)